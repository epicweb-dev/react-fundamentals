# Checkbox

<EpicVideo url="https://www.epicreact.dev/workshops/react-fundamentals/checkbox/solution" />

👨‍💼 Great job! Interesting (weird) how checkboxes work huh?

You may have also noticed that the `age` input is a string even though its
`type` attribute is `number`. It's good to understand how values are submitted
for different input types!
